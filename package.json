{
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare",
		"jsonapi": "npx json-server --watch jsonapi/db.json --p 8000"
	},
	"devDependencies": {
		"nuxt": "3.0.0"
	},
	"dependencies": {
		"json-server": "^0.17.1",
		"mdi": "^2.2.43",
		"sass": "^1.56.2",
		"stylus": "^0.59.0",
		"stylus-loader": "^7.1.0",
		"vuetify": "^3.0.4"
	}
}
